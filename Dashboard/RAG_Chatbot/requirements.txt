flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
google-generativeai>=0.8.0
openai>=1.0.0
# Optional: semantic embeddings (falls back gracefully if missing)
sentence-transformers>=2.2.0
numpy>=1.26.0
# transformers and torch are pulled by sentence-transformers when needed
